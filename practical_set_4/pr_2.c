#include <stdio.h>

int main()
{
    int i;
    for (int i = 17; i <= 45; i++)
    {
        if (i % 5 == 0)

        {
            printf("multiple of 5 range is = %d \n", i);
        }
    }
    return 0;
}