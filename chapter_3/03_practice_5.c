#include<stdio.h>

int main(){
     int number;
     printf("Enter the number\n");
     scanf("%d", &number);

     number>4;

     if(number>4)
     {
        printf("The number is greater than 4 is %d", number); 
     }

     else{
         printf("The number is not greater than 4 is %d", number);  
     }
    return 0;
}